# Debugging Protocol Template (H1/H2 + one test)

## Symptom (verbatim)
Paste the exact error message / behavior. Include the command that produced it.

## Environment
- OS + version:
- Hardware (if relevant):
- App/tool + version:
- Network context (if relevant):

## Hypotheses
- **H1 (most likely):**
- **H2 (closest alternative):**

## Discriminating test (one)
Command to run (replace the line below with the exact command):
```bash
PASTE_COMMAND_HERE
```

Expected outcomes:
- If result is **A**, supports **H1** because [...]
- If result is **B**, supports **H2** because [...]

## Next action
- If H1: [...]
- If H2: [...]
